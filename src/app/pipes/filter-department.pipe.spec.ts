import { FilterDepartmentPipe } from './filter-department.pipe';

describe('FilterDepartmentPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDepartmentPipe();
    expect(pipe).toBeTruthy();
  });
});
